{
  "name": "transliteration",
  "version": "1.0.0",
  "description": "RUN git clone https://github.com/libindic/indic-trans.git \\ && cd indic-trans \\ && pip3 install -r requirements.txt \\ && python3 setup.py install \\ && cd ..",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "mysql2": "^3.7.0",
    "node-cmd": "^5.0.0"
  }
}
